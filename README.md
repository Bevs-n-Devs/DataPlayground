# DataPlayground
Projects involving data collection, cleaning, and ML/AI


### Environment Setup:
* Python 3.8 or newer
* Chrome Browser
* Python packages
    * run the following
    `python -m pip install -r requirements.txt`